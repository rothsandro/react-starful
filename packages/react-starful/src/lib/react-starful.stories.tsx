import type { Meta } from '@storybook/react';
import { ReactStarful } from './react-starful';

const Story: Meta<typeof ReactStarful> = {
  component: ReactStarful,
  title: 'ReactStarful',
};
export default Story;

export const Primary = {
  args: {},
};
